# 数据采集模块 — 需求分析用例图

```mermaid
graph LR
    subgraph 系统边界["数据采集模块"]
        direction TB

        %% ===== 第一列：认证 =====
        subgraph 认证层[" "]
            direction TB
            登录认证["🔐 登录认证<br/>(JWT Token)"]
        end

        %% ===== 第二列：核心用例 =====
        subgraph 核心用例层[" "]
            direction TB
            采集智能评估数据["📋 采集智能评估数据<br/>认知筛查/运动功能/生命体征"]
            采集健康问询数据["📋 采集健康问询数据<br/>既往病史/家族病史/生活方式/症状自查"]
            采集影像报告数据["📋 采集影像报告数据<br/>CT/MRI/X-Ray/超声"]
        end

        %% ===== 第三列：CRUD 子用例 =====
        subgraph CRUD层[" "]
            direction TB
            新增数据["新增数据"]
            查看数据["查看数据"]
            修改数据["修改数据"]
            删除数据["删除数据"]
        end

        %% ===== 第四列：查询统计 =====
        subgraph 查询统计层[" "]
            direction TB
            查询与过滤["🔍 查询与过滤<br/>按老人/类型/日期筛选"]
            获取最新评估["📊 获取最新评估结果"]
            统计影像报告数["📊 统计影像报告数量"]
            数据统计概览["📈 数据统计概览<br/>各渠道/今日/本周/本月统计"]
            获取数据字典["📖 获取数据字典<br/>数据来源/数据类型枚举"]
        end

        %% ===== include 关系（垂直方向） =====
        采集智能评估数据 -.->|include| 新增数据
        采集智能评估数据 -.->|include| 查看数据
        采集智能评估数据 -.->|include| 修改数据
        采集智能评估数据 -.->|include| 删除数据

        采集健康问询数据 -.->|include| 新增数据
        采集健康问询数据 -.->|include| 查看数据
        采集健康问询数据 -.->|include| 修改数据
        采集健康问询数据 -.->|include| 删除数据

        采集影像报告数据 -.->|include| 新增数据
        采集影像报告数据 -.->|include| 查看数据
        采集影像报告数据 -.->|include| 修改数据
        采集影像报告数据 -.->|include| 删除数据

        查询与过滤 -.->|include| 查看数据

        %% ===== extend 关系 =====
        新增数据 ..->|extend| 登录认证
        查看数据 ..->|extend| 登录认证
        修改数据 ..->|extend| 登录认证
        删除数据 ..->|extend| 登录认证
        查询与过滤 ..->|extend| 登录认证
        获取最新评估 ..->|extend| 登录认证
        统计影像报告数 ..->|extend| 登录认证
        数据统计概览 ..->|extend| 登录认证
        获取数据字典 ..->|extend| 登录认证
    end

    %% ===== 参与者（左侧纵向排列） =====
    subgraph 参与者区[" "]
        direction TB
        前端系统["🖥️ Vue3 前端系统"]
        管理员["👤 管理员 (admin)"]
        医生["👤 医生 (doctor)"]
        护士["👤 护士 (nurse)"]
    end

    前端系统 -.-> 管理员
    前端系统 -.-> 医生
    前端系统 -.-> 护士

    %% ===== 关联：参与者 → 核心用例 =====
    管理员 --> 采集智能评估数据
    管理员 --> 采集健康问询数据
    管理员 --> 采集影像报告数据
    管理员 --> 查询与过滤
    管理员 --> 获取最新评估
    管理员 --> 统计影像报告数
    管理员 --> 数据统计概览
    管理员 --> 获取数据字典

    医生 --> 采集智能评估数据
    医生 --> 采集健康问询数据
    医生 --> 采集影像报告数据
    医生 --> 查询与过滤
    医生 --> 获取最新评估
    医生 --> 统计影像报告数
    医生 --> 数据统计概览
    医生 --> 获取数据字典

    护士 --> 采集智能评估数据
    护士 --> 采集健康问询数据
    护士 --> 采集影像报告数据
    护士 --> 查询与过滤
    护士 --> 获取最新评估
    护士 --> 统计影像报告数
    护士 --> 数据统计概览
    护士 --> 获取数据字典

    %% ===== 样式 =====
    style 系统边界 fill:#f5f5f5,stroke:#333,stroke-width:2px
    style 登录认证 fill:#ffcccc,stroke:#900
    style 采集智能评估数据 fill:#e0f0ff,stroke:#369
    style 采集健康问询数据 fill:#e0f0ff,stroke:#369
    style 采集影像报告数据 fill:#e0f0ff,stroke:#369
    style 新增数据 fill:#e8f5e9,stroke:#4a4
    style 查看数据 fill:#e8f5e9,stroke:#4a4
    style 修改数据 fill:#e8f5e9,stroke:#4a4
    style 删除数据 fill:#e8f5e9,stroke:#4a4
    style 查询与过滤 fill:#fff3e0,stroke:#f80
    style 数据统计概览 fill:#fff3e0,stroke:#f80
```

---

## 参与者说明

| 参与者 | 类型 | 权限范围 |
|--------|------|----------|
| **管理员 (admin)** | 主参与者 | 全部权限：增/删/改/查 + 统计 + 字典 |
| **医生 (doctor)** | 主参与者 | 增/改/查 + 统计 + 字典（部分删除受限） |
| **护士 (nurse)** | 主参与者 | 仅新增 + 查看 + 统计 + 字典 |
| **Vue3 前端系统** | 辅助参与者 | 为各角色提供 UI 界面，转发请求 |
| **JWT 认证服务** | 辅助参与者 | Token 签发与校验，所有接口的前置条件 |

## 用例说明

| 用例 | 说明 |
|------|------|
| 采集智能评估数据 | 采集三类评估（认知筛查、运动功能、生命体征），含评估项、总分、等级、建议 |
| 采集健康问询数据 | 采集四类问询（既往病史、家族病史、生活方式、症状自查），含问答 JSON、风险分析 |
| 采集影像报告数据 | 采集五类影像（CT/MRI/X-Ray/超声/其他），含影像 URL、诊断结果、异常指标 |
| 查询与过滤 | 按老人 ID、数据来源、数据类型、日期范围、采集人组合筛选 |
| 获取最新评估 | 按老人 ID + 评估类型获取最新一条评估记录 |
| 统计影像报告数 | 按老人 ID 统计该老人的影像报告数量 |
| 数据统计概览 | 返回各渠道总量、今日/本周/本月新增量 |
| 获取数据字典 | 暴露 DataSourceEnum / DataTypeEnum 枚举值供前端下拉菜单使用 |

## 关系说明

| 关系 | 含义 |
|------|------|
| **关联** (实线) | 参与者与用例之间存在交互 |
| **include** (虚线箭头 `<<include>>`) | 复合用例必然调用子用例，表示功能复用 |
| **extend** (虚线箭头 `<<extend>>`) | 在特定条件下（此处为已登录）扩展基础用例行为 |

> 注：Mermaid `graph TB` 模式对用例图样式的支持有限，图中用 `include`/`extend` 标注关系。如需要标准 UML 用例图渲染，可将此图导出到 PlantUML 或 draw.io 中重绘。
